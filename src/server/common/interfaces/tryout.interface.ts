import { BaseResponse } from 'src/shared/base-response';
import { AuthLoginDTO, AuthRegisterDTO } from 'src/shared/dtos';

export interface ITryoutService {
   // medexLogin(payload: TryoutLoginDTO): Promise<BaseResponse>;
   // medexRegister(payload: TryoutRegisterDTO): Promise<BaseResponse>;
}
